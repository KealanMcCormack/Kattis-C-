#include <iostream>
using namespace std;
int main(void)
{
 string a;
cin >> a;
    for(int count=1;count<a.length();count++){
      if(a.at(count - 1) == a.at(count)){
        a.erase(count - 1, 1);
        count--;
      }
    }
    cout << a;
    return 0;
}
